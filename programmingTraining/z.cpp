#include <bits/stdc++.h>

using namespace std;

void gifts(vector <int> v) {
    for (int i = 0; i < v.size(); ++i) {
        if (v[i] == 0) {
            for (int j = 0; j < v.size(); ++j) {
                if (i + 1 != v[i]) {
                    
                }
            }
        }
    }
}

int main() {
    int n; cin >> n;
    int k = 1;
    vector <int> v;
    while (n--) {
        int x; cin >> x;
        v.push_back(x);
    }

    return 0;
}